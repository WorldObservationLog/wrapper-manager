services:
  wrapper-manager:
    build: .
    container_name: wrapper-manager
    command: [
      "--host", "0.0.0.0",
      "--port", "8080",
      "--mirror", "false"
    ]
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
